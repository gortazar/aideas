# Generated from ideas/_template/ci.yml. Edit freely — it is only created once, never
# regenerated, so your changes to the real test command will stick.
name: CI - rescued

on:
  push:
    paths:
      - "ideas/rescued/**"
      - ".github/workflows/ci-rescued.yml"
  pull_request:
    paths:
      - "ideas/rescued/**"
      - ".github/workflows/ci-rescued.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ideas/rescued
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v27
        with:
          extra_nix_config: "experimental-features = nix-command flakes"
      - run: nix flake check

# Generated from ideas/_template/ci.yml. Edit freely — it is only created once, never
# regenerated, so your changes to the real test command will stick.
name: CI - restore-wss

on:
  push:
    paths:
      - "ideas/restore-wss/**"
      - ".github/workflows/ci-restore-wss.yml"
  pull_request:
    paths:
      - "ideas/restore-wss/**"
      - ".github/workflows/ci-restore-wss.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ideas/restore-wss
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v27
        with:
          extra_nix_config: "experimental-features = nix-command flakes"
      - run: nix flake check
